@model IEnumerable<RDFindAuto.Models.TipoCombustible>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tipoCombustibledesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tipoCombustibledesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDTipoCombustible }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDTipoCombustible }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDTipoCombustible })
        </td>
    </tr>
}

</table>
