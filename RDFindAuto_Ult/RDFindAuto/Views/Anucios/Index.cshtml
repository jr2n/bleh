@model IEnumerable<RDFindAuto.Models.Anucio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.anucioDesc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.URL)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.anucioDesc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.URL)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDAnucio }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDAnucio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDAnucio })
        </td>
    </tr>
}

</table>
