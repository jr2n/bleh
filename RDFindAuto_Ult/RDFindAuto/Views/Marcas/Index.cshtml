@model IEnumerable<RDFindAuto.Models.Marca>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.marcaDesc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.URLImg)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.marcaDesc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.URLImg)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDMarca }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDMarca }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDMarca })
        </td>
    </tr>
}

</table>
