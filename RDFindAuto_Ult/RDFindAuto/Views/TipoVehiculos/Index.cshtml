@model IEnumerable<RDFindAuto.Models.TipoVehiculo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tipoVehiculodesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tipoVehiculodesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDTipoVehiculo }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDTipoVehiculo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDTipoVehiculo })
        </td>
    </tr>
}

</table>
